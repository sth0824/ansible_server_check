# DB 데이터 마이그레이션 설명

## 배포 과정

### 1단계: 프로젝트 파일 배포
- **스크립트**: `deploy_to_nimbus.sh`
- **내용**: 
  - 로컬 `api_server/` 디렉토리 → 서버 `/opt/ansible-monitoring/api_server/`
  - Python 코드, HTML 템플릿 등
- **DB 데이터**: 포함 안 됨 ❌

### 2단계: 서버 환경 구축
- **스크립트**: `deploy_install.sh` (서버에서 실행)
- **내용**:
  - Python 3.9 설치
  - PostgreSQL 설치
  - 빈 데이터베이스 생성 (`ansible_checks`)
  - Python 패키지 설치
- **DB 데이터**: 빈 DB만 생성, 데이터 없음 ❌

### 3단계: DB 데이터 마이그레이션 (별도 작업)
- **스크립트**: `migrate_db_to_server.sh` (만들어놨지만 실행 여부 불명확)
- **방법**:
  1. 로컬 DB에서 데이터 덤프 (`pg_dump`)
  2. 서버로 전송
  3. 서버 DB에 복원 (`psql`)

## 현재 상황

- **로컬 DB**: 108건 (ID 1-139)
- **서버 DB**: 108건 (확인됨)

## 가능한 시나리오

### 시나리오 1: 마이그레이션 스크립트 실행함
- `migrate_db_to_server.sh` 실행했을 가능성
- 로컬 → 서버로 데이터 복사됨

### 시나리오 2: 수동으로 마이그레이션함
- 직접 `pg_dump` / `psql` 명령어로 마이그레이션했을 가능성

### 시나리오 3: 다른 방법
- 다른 스크립트나 방법으로 마이그레이션했을 가능성

## 확인 방법

서버 DB의 최초 데이터 생성 시간을 확인하면 마이그레이션 시점을 알 수 있습니다.
