# DB 데이터 마이그레이션 상태

## 배포 과정 요약

### 1. 프로젝트 파일 배포 (`deploy_to_nimbus.sh`)
- ✅ 로컬 `api_server/` → 서버 `/opt/ansible-monitoring/api_server/`
- ❌ **DB 데이터는 포함 안 됨** (코드만 업로드)

### 2. 서버 환경 구축 (`deploy_install.sh`)
- ✅ Python 3.9 설치
- ✅ PostgreSQL 설치
- ✅ **빈 데이터베이스 생성** (`ansible_checks`)
- ❌ **데이터는 없음** (테이블만 생성)

### 3. DB 데이터 마이그레이션
- **스크립트**: `migrate_db_to_server.sh` (준비됨)
- **실행 여부**: 불명확
- **방법**: 
  1. 로컬 DB에서 덤프 (`pg_dump`)
  2. 서버로 전송
  3. 서버 DB에 복원 (`psql`)

## 현재 상태

- **로컬 DB**: 108건 (2025-12-29 ~ 2026-01-22)
- **서버 DB**: 108건 (확인됨)

## 결론

**서버에 DB 데이터가 있는 이유:**
1. 마이그레이션 스크립트를 실행했거나
2. 수동으로 데이터를 복사했거나
3. 다른 방법으로 마이그레이션했을 가능성

**배포 스크립트 자체는 DB 데이터를 복사하지 않습니다.**
- 배포 = 프로젝트 코드만 업로드
- DB 데이터 = 별도 마이그레이션 필요
