# 와이파이에서 포트 1025 접근 문제 해결

## 문제 상황

- **핫스팟**: 포트 1025 접근 가능 ✅
- **와이파이**: 포트 1025 접근 불가 ❌
- **ACG 설정**: 포트 1025 전체 허용 (0.0.0.0/0) ✅

## 가능한 원인

1. **와이파이 라우터/방화벽**: 라우터가 특정 포트를 차단
2. **ISP(인터넷 제공자) 차단**: 통신사에서 비표준 포트 차단
3. **NAT/방화벽 설정**: 네트워크 장비의 보안 정책

## 해결 방법

### 방법 1: SSH 터널링 (권장)

핫스팟으로 먼저 접속한 후, SSH 터널을 만들어 와이파이에서 사용:

```bash
# 핫스팟에서 실행 (백그라운드)
ssh -i ~/.ssh/nimso2026.pem -p 1025 -N -L 2222:localhost:22 root@27.96.129.114 &

# 와이파이에서 로컬 포트 2222로 접속
ssh -i ~/.ssh/nimso2026.pem -p 2222 root@localhost
```

### 방법 2: VPN 사용

VPN을 통해 접속하면 포트 제한을 우회할 수 있습니다.

### 방법 3: 표준 포트 사용 (포트 22)

포트 22는 대부분의 네트워크에서 허용됩니다:

1. **네이버 클라우드 콘솔**에서 포트 포워딩 설정
2. **외부 포트 22 → 내부 포트 22** 매핑 생성
3. 배포 스크립트에서 포트 22 사용

### 방법 4: 라우터 설정 확인

와이파이 라우터 관리 페이지에서:
- 포트 포워딩 설정 확인
- 방화벽 설정 확인
- 포트 필터링 해제

### 방법 5: 다른 포트 사용

ACG에서 다른 표준 포트(예: 443, 8443)를 사용:

1. 네이버 클라우드에서 포트 포워딩: 외부 포트 443 → 내부 포트 22
2. ACG에서 포트 443 허용
3. 배포 스크립트 수정

---

## 임시 해결책: 핫스팟으로 배포

와이파이 문제를 해결하는 동안, 핫스팟으로 배포를 진행할 수 있습니다:

```bash
# 핫스팟 연결 후
cd /home/sth0824/ansible
./deploy_to_nimbus.sh
```

배포는 한 번만 하면 되므로, 핫스팟으로 배포 후 와이파이에서는 접속만 하면 됩니다.

---

## 추천 해결책

**포트 22 사용**이 가장 안정적입니다:
- 대부분의 네트워크에서 허용
- 표준 SSH 포트
- 포트 포워딩 설정만 추가하면 됨

포트 22로 변경할까요?
