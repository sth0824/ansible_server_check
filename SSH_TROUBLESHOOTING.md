# SSH 접속 문제 해결 가이드

## 현재 문제

포트 1025로 SSH 접속이 타임아웃되고 있습니다.

```
ssh: connect to host 27.96.129.114 port 1025: Connection timed out
```

## 가능한 원인

1. **ACG(방화벽) 설정**: 현재 IP가 포트 1025 접근이 허용되지 않음
2. **포트 포워딩 설정 변경**: 네이버 클라우드에서 포트 포워딩이 변경되었을 수 있음
3. **네트워크 문제**: 일시적인 네트워크 문제

## 해결 방법

### 1. 네이버 클라우드 콘솔에서 확인

1. **Server > nimbus-server > ACG 규칙 보기**
2. **인바운드 규칙 확인**:
   - 포트 1025가 현재 IP에 허용되어 있는지 확인
   - 또는 포트 22가 현재 IP에 허용되어 있는지 확인

### 2. 포트 포워딩 확인

1. **Server > nimbus-server > 포트 포워딩 설정**
2. 포트 포워딩이 여전히 활성화되어 있는지 확인
3. 외부 포트 1025 → 내부 포트 22 매핑 확인

### 3. 현재 IP 확인

```bash
# 현재 공인 IP 확인
curl ifconfig.me
```

이 IP가 ACG에 허용되어 있는지 확인하세요.

### 4. 포트 22로 직접 접속 시도

ACG에서 포트 22가 현재 IP에 허용되어 있다면:

```bash
ssh -i ~/.ssh/nimso2026.pem -p 22 root@27.96.129.114
```

성공하면 배포 스크립트의 포트를 22로 변경하세요.

### 5. 배포 스크립트 포트 변경

포트 22로 접속이 가능하다면:

```bash
# deploy_to_nimbus.sh 파일 수정
sed -i 's/SERVER_PORT="1025"/SERVER_PORT="22"/' deploy_to_nimbus.sh
```

---

## 임시 해결책

ACG 설정을 변경할 수 없다면:

1. **네이버 클라우드 콘솔 접속**
2. **Server > nimbus-server > ACG 규칙 보기**
3. **인바운드 규칙 추가**:
   - 포트: `1025` (또는 `22`)
   - 프로토콜: `TCP`
   - 소스: 현재 IP 또는 `0.0.0.0/0` (전체 허용 - 보안 주의)
   - 설명: `SSH Access`

---

## 확인 후 다음 단계

SSH 접속이 성공하면 배포 스크립트를 다시 실행하세요:

```bash
./deploy_to_nimbus.sh
```
