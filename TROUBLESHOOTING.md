# 외부 접속 문제 해결 가이드

## 현재 상황

✅ **서버 내부**: 정상 작동
- API 서버 실행 중
- 포트 8000 리스닝 중
- 로컬 접속 성공

❌ **외부 접속**: 실패
- Connection refused
- ACG 설정은 되어 있음
- FirewallD 포트 8000 허용됨

## 가능한 원인

### 1. ACG 설정 문제
- ACG 규칙이 실제로 적용되지 않았을 수 있음
- ACG가 서버에 제대로 연결되지 않았을 수 있음

### 2. 포트 포워딩 필요
- 네이버 클라우드에서 포트 포워딩 설정이 필요할 수 있음
- 외부 포트 8000 → 내부 포트 8000

### 3. 서버의 공인 IP 확인
- 실제 공인 IP가 27.96.129.114가 아닐 수 있음

### 4. 네이버 클라우드 네트워크 설정
- 서브넷 설정 문제
- 라우팅 문제

## 해결 방법

### 방법 1: ACG 재설정
1. 네이버 클라우드 콘솔 접속
2. Server > nimbus-server > ACG 규칙 보기
3. 포트 8000 규칙 삭제 후 재추가
4. 적용 후 서버 재시작

### 방법 2: 포트 포워딩 설정
1. 네이버 클라우드 콘솔
2. Server > nimbus-server > 포트 포워딩 설정
3. 외부 포트 8000 → 내부 포트 8000 추가
4. 외부 접속 시 포트 포워딩 IP 사용

### 방법 3: 서버 공인 IP 확인
서버에서 실행:
```bash
curl ifconfig.me
```

이 IP가 27.96.129.114와 일치하는지 확인

### 방법 4: 네이버 클라우드 지원팀 문의
위 방법으로 해결되지 않으면 네이버 클라우드 지원팀에 문의
