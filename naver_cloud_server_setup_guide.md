# 네이버 클라우드 서버 생성 가이드
## API 서버 + PostgreSQL DB 서버 배포용

---

## 1단계: 서버 이미지 선택

### ✅ 추천 OS 이미지

**1순위: `ubuntu-24.04` (Ubuntu 24.04 LTS)**
- **이유**: 
  - 최신 LTS 버전으로 안정적
  - Python 3.12 기본 설치 (프로젝트 요구사항: Python 3.8+)
  - 패키지 관리가 쉬움 (apt)
  - 커뮤니티 지원 활발
  - PostgreSQL 설치 및 설정이 간편

**2순위: `rocky-9.6-base` (Rocky Linux 9.6)**
- **이유**:
  - RHEL 계열로 안정적
  - 엔터프라이즈 환경에 적합
  - Python 설치 필요 (yum/dnf)

**선택하지 말 것:**
- Windows Server (Linux 기반 프로젝트)
- 구버전 OS (보안 및 호환성 문제)

---

## 2단계: 서버 설정

### 서버 사양 추천

**최소 사양 (테스트/개발용):**
- **CPU**: 2 vCPU
- **메모리**: 4GB RAM
- **이유**: API 서버 + PostgreSQL 동시 운영 가능

**권장 사양 (운영용):**
- **CPU**: 4 vCPU
- **메모리**: 8GB RAM
- **이유**: 
  - API 서버: ~500MB
  - PostgreSQL: ~1-2GB
  - 시스템 여유: ~2GB
  - 점검 데이터 증가 대비

**고사양 (대용량 데이터):**
- **CPU**: 8 vCPU
- **메모리**: 16GB RAM

### 서버 이름
- 예: `ansible-api-server` 또는 `monitoring-server`

---

## 3단계: 스토리지 설정

### 스토리지 타입
- **SSD**: 권장 (빠른 DB I/O)

### 스토리지 용량
- **최소**: 50GB
- **권장**: 100GB
  - OS: ~20GB
  - PostgreSQL 데이터: ~20-30GB
  - 로그 파일: ~10GB
  - 여유 공간: ~40GB

### 스토리지 추가 (선택)
- 필요시 데이터 전용 스토리지 추가 가능

---

## 4단계: 인증키 설정

### 옵션 1: 기존 인증키 사용
- `nimso2026.pem` (이미 nimbus-server에서 사용 중)
- 동일한 키로 관리 통일

### 옵션 2: 새 인증키 생성
- 새 키 생성 후 다운로드
- 로컬에 저장: `/home/sth0824/.ssh/new_key.pem`
- 권한 설정: `chmod 600 new_key.pem`

---

## 5단계: 네트워크 접근 설정 (중요!)

### ACG (Access Control Group) 설정

**필요한 포트:**
1. **SSH (22)**: 서버 관리용
   - 현재 IP만 허용 (보안)
2. **API 서버 (8000)**: 대시보드 접근용
   - 전체 허용 또는 특정 IP만 허용
3. **PostgreSQL (5432)**: DB 접근용 (선택)
   - 내부 네트워크만 허용 (보안 권장)
   - 또는 특정 IP만 허용

### 포트 포워딩 (선택)
- 현재 nimbus-server처럼 포트 포워딩 사용 가능
- 예: 외부 8001 → 내부 8000

---

## 6단계: 최종 확인

### 확인 사항
- [ ] OS 이미지: Ubuntu 24.04 선택
- [ ] 서버 사양: 최소 2 vCPU, 4GB RAM
- [ ] 스토리지: 최소 50GB SSD
- [ ] 인증키: 기존 키 또는 새 키
- [ ] ACG: 포트 22, 8000 설정
- [ ] 서버 이름: 적절한 이름 지정

---

## 서버 생성 후 다음 단계

1. **SSH 접속 확인**
2. **시스템 업데이트**
3. **Python 3.8+ 설치 확인**
4. **PostgreSQL 설치 및 설정**
5. **프로젝트 파일 업로드**
6. **API 서버 및 DB 서버 설정**
7. **systemd 서비스 등록 (자동 시작)**
8. **방화벽/ACG 확인**

---

## 예상 비용 (참고)

- **서버 (2 vCPU, 4GB, 50GB)**: 월 약 5-7만원
- **서버 (4 vCPU, 8GB, 100GB)**: 월 약 10-15만원
- **네트워크 트래픽**: 사용량에 따라 추가

---

## 주의사항

⚠️ **보안**
- SSH 포트는 특정 IP만 허용
- PostgreSQL 포트는 내부 네트워크만 허용 (가능하면)
- API 서버 포트는 필요시에만 전체 허용

⚠️ **백업**
- 정기적인 DB 백업 설정 권장
- 스냅샷 기능 활용

⚠️ **모니터링**
- 서버 리소스 사용량 모니터링
- 로그 파일 관리
